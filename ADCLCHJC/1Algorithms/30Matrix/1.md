# Theory


1. Rotate 90degrees clockwise


Algorithm
1. Transpose(rows become columns)


squares


The rows and columns interchange, we have to do it for half the matric only, because when you swap, the elements swap at a time


```python
for(int r=0;r<rr;r++){
    for(int c=r+1;c<cc;c++){
        swap(ma[r][c],ma[c][r]);
    }
}
```

For rectangle


```python
vector<vector<int>> ans(cc,vector<int>(rr));
for(int r=0;r<rr;r++){
    for(int c=r+1;c<cc;c++){
        ans[c][r]=ma[r][c];
    }
}
```

Mirror image verticalaly
1. reverse each row


![image.png](1_images/image.png)


# Reverse Lookup for a matrix


(i+1)(j+1)(n-i)(m-j) * ma[i][j]




