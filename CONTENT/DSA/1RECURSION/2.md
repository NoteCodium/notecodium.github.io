# n^p || Binary exponentiation
```
n^k=n^(k/2)*n^(k/2) //if k is even
n^k=n^(k/2)*n^(k/2)*n //if k is odd
```

```cpp
int pow(n,k){
  if(k==0){
    return 1;
  }
  int x=pow(n,k/2);
  if(k%2==0){
    return x*x;
  }
  return x*x*n;
}
```
